POSTGRES_USER=stepfit
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=stepfit

DATABASE_URL=postgresql://stepfit:your_password_here@localhost:5433/stepfit

PORT=4000
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:5173

# Generate each with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=
COOKIE_SECRET=
MFA_ENCRYPTION_KEY=